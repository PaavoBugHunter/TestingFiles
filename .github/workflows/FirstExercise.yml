name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: windows-latest
    steps:
      - name: echo a string
        run: echo "Hello Paavo!"
      - name: multipline script
        run: |
          python --version
          node -v
          